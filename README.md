# vcert-cli-playground
This repository contains working examples and 'try it out' features for learning and testing vCert CLI.

## Installing vcert
From the Dev Container, run `sudo ./common/get-vcert.sh` to install the latest vcert.